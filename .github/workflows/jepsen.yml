name: Jepsen tests
on:
  workflow_dispatch:
    inputs:
      retainCluster:
        description: 'Keeps the AWS Jepsen worker cluster running after the test is complete. Subsequent test runs will reuse it.'
        required: false
        default: true # TODO: change to false before merging
        type: boolean
      workloads:
        description: 'List of workloads to run'
        required: false
        type: string
      nemeses:
        description: 'List of nemeses to apply'
        required: false
        type: string
  push:
    branches: [ "main", "github-action" ]

permissions:
  id-token: write
  contents: read

jobs:
  jepsen-tests:
    runs-on: warp-ubuntu-latest-x64-2x

    steps:
      - uses: actions/checkout@v4

      - uses: extractions/setup-just@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - uses: DeLaGuardo/setup-clojure@13.0
        with:
          lein: 2.11.2

      - run: sudo apt-get update && sudo apt-get install -y gnuplot

      - name: Build SDK services
        run: just make-services

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: us-east-1
          role-to-assume: arn:aws:iam::339713187748:role/github-restatedev-jepsen-actions-role

      # TODO: restrict the cluster to only be accessible to the build worker
      - run: curl -s https://checkip.amazonaws.com

      - name: Create test cluster
        run: just create-aws-cluster

      - name: Run Jepsen tests
        run: |
          status=0
          for workload in ${{ inputs.workloads || 'set-vo set-mds' }}; do
            for nemesis in ${{ inputs.nemeses || 'none partition-random-node container-killer' }}; do
              lein run test \
                --nodes-file aws/nodes.txt --username admin --ssh-private-key aws/private-key.pem \
                --image ghcr.io/restatedev/restate:main \
                --workload ${workload} --nemesis ${nemesis} \
                --time-limit 30 --rate 10 --concurrency 5n
              if [ $? -ne 0 ]; then
                echo "::error::Test failed"
                status=1
              fi
            done
          done
          exit ${status}
        continue-on-error: true
        timeout-minutes: 10

      - name: Pack Jepsen store directory
        run: tar cvfz jepsen-store.tar.gz store

      - name: Upload store artifact
        uses: actions/upload-artifact@v3
        with:
          name: jepsen-store
          path: jepsen-store.tar.gz

      - name: Destroy cluster
        if: ${{ !(inputs.retainCluster || true) }} # TODO: update default before merging
        run: just destroy-aws-cluster

      - name: Set overall job status
        if: ${{ steps.jepsen-tests.outcome == 'failure' }}
        run: exit 1
